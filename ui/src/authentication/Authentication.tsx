import React from "react";
import { Card } from "react-bootstrap";

const Authentication = () => {
	
	return (
		<Card>Authentication contents to go here.</Card>
	);
};

export default Authentication;